# Server Config
server.port=${PORT:8085}

# Database Config (Neon PostgreSQL)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-little-dream-a1mdbp1e-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_YSabUDOz8C7V}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# JWT Config
jwt.secret=${JWT_SECRET:your-super-secret-key-at-least-256-bits-long-for-hs256}
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=INFO
